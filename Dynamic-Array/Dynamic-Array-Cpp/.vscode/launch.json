{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CMake Build",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/dynamic_array.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "externalTerminal",
            "preLaunchTask": "cmake build"
        }
    ],
    "tasks": {
        "version": "2.0.0",
        "tasks": [
            {
                "label": "cmake build",std::
                "type": "shell",
                "command": "cmake --build ${command:cmake.buildDirectory} --config Debug",
                "group": {
                    "kind": "build",
                    "isDefault": true
                }
            }
        ]
    }
}
